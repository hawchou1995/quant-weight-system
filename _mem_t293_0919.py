# -*- coding: utf-8 -*-
"""记忆：R-topic293 结论 + 4 条新陷阱 + 索引更新。"""
import sys
from pathlib import Path

sys.stdout.reconfigure(encoding="utf-8")
MEM = Path(r"C:/Users/Admin/.zcode/cli/memories/projects/project-9022da3c47c5e5bd/memory")

# ---------- 1) 量化状态记忆 ----------
p = MEM / "project-quant-system-state.md"
s = p.read_text(encoding="utf-8")
SEC = """

## ★★ R-topic293 gushi「两点半 选股经验」判负期望（2026-09-19）

**来源**：gushi.in/topic/293（14:30 平台名单之上加 ①涨幅带 1~3% ②14:45 不回落≥−0.2% ③取涨幅最高一只 ④无则空仓）。预注册 `backtest/PRE-REGISTRATION_20260919_topic293.md`（54 臂冻结），报告 `backtest/报告-topic293两点半滤网正负期望-20260919.md`。

**判负，三条规则逐条为负/零**（数据：pytdx 分时 idx209=14:30/idx224=14:45，实测与 5 分钟线逐点相等；日线 data_full 前复权；收益走同日比值）
- **F1（1~3% 带）负**：全市场带内人群 **21,890 笔/42 抽样日**：买 14:45→次日开盘 **−0.59%/笔（胜率 17.7%）**、次日收盘 −0.39%；**分年单调恶化**（次日收盘 2024 +0.81% → 2025 −0.08% → 2026 **−1.30%**）；50bp 档全负。
- **F2（不回落）方向反**：按回落分档（n=21,890）—— 回落 −2%~−0.5% 次日 **−0.209%（胜率 45.7%）**、继续涨 +0.5%~+5% 的 **−1.302%（胜率 26.8%）**；F2 通过 vs 拒绝（次日收盘）−0.452% vs **−0.197%** → **原帖要求"不能回落"恰好挑中较差的一半**。
- **Top1 带内方向反**：corr(14:30 涨幅, 次日收益) = **−0.063**；Top1 比全买更差。
- **唯一正期望证据**：平台真实名单 15 天 / 48 行 / n=9 → 买 14:45→**次日收盘 +2.72%/笔、胜率 78%**（同期全市场带 −1.30% ⇒ 差值=名单选股能力）→ **n=9 不可采信**，继续逐日积累待 n≥100 重判。
- **可移植性证伪**：F1 与 KHunter 主信号（RSI<35 超跌）**互斥，通过率仅 4%（13/309）** → 该滤网不能移植到我们三池。

**★ 顺带真信号（新候选线索，零生产变更）**：同一动量名单上 **14:30 涨幅 ≥9.5%（涨停/准涨停）→ 次日开盘 +2.74%/笔、胜率 61.1%（聚类 t +12.65）**；剔除 14:45 已封板买不进（占 51%）后**可成交子集仍 +1.73%/笔、胜率 54.2%（t +5.74）**。**这是原帖 F1 明确排除的区域，与我方打板族同域** → 建议交打板线另立预注册评估（不得与本报告混算）。

**产物**：`backtest/t293_minute_0919.py`（分时层，自检 5/5）｜`t293_l2_real_list_0919.py`（忠实臂）｜`t293_l1_proxy_0919.py`（KHunter 代理臂）｜`t293_c_l3_0919.py`（动量复刻 C + 全市场带 L3）｜`t293_eval_0919.py`（单遍求值器）｜`t293_{l2,l1,l3,c}_rows.csv` + `_summary.json`；分时缓存 `D:/Tools/cache/tdx_min/`（+约 2.5 万 parquet）。n_trials 申报 **58**。
"""
s = s.rstrip() + "\n" + SEC
p.write_text(s, encoding="utf-8")
print("project-quant-system-state.md：追加 R-topic293 段")

# ---------- 2) 陷阱库 ----------
p2 = MEM / "project-engineering-traps.md"
s2 = p2.read_text(encoding="utf-8")
line0 = s2.split("\n", 1)[0]
TRAP = """
> 2026-09-19 新增（R-topic293 回测 · 四条工程坑）：
> - **★★「名单代理」臂必须先验通过率，否则整条臂白跑**：用 KHunter 主信号（RSI<35 超跌反转）当"平台推荐名单"的代理，去套原帖的「当日涨幅 1~3%」滤网 → **通过率仅 4%（13/309）**，两条规则天然互斥（超跌股当天大多在跌）→ 该臂只能在 n=12 上给出"不可判定"。**规则：代理名单与待测滤网的"状态相容性"要先用一次计数验证（几十秒的事），相容性差就换载体，别把整轮算力砸在 n=12 上。**
> - **★★ 同一天内用"全天信息"构造名单 = 轻度前视**：复刻平台名单时用了缓存里的 `vol_ratio`（当日全天量比）与 `ret20`（当日收盘派生）→ 在 14:30 决策时**不可知** ⇒ 该臂水平数只能当"上界"，结论只能取**同名单内的相对对比**。**规则：任何"日内时点"策略，先列出每个特征在决策时点是否已可知，不可知的要么换成当日已实现量（如 open_rise/14:30 涨幅），要么显式降级为"上界"。**
> - **★★ 求值器写成 O(n²) → 21k 行要跑几小时**：旧 evaluate 对每个臂都 `bdf.to_dict("records")` + 每次 `list(d.index)`（O(1300)），21,890 行 × 48 臂 = 百万次线性扫描 → ETA 数小时。正解=**单遍求值**：每票日线索引只建一次（dict 缓存 + date→pos），每个 (执行价, 出场, 成本档) 只算一遍，4 个基线退化为对同一批收益做子集聚合 → 秒级。**规则：回测求值前先估复杂度（臂数 × 行数 × 每行 O(索引长度)），>1e8 就先重构。**
> - **★ 正向最漂亮的档位要先过"可成交性"**：剂量曲线显示 14:30 涨幅 ≥9.5% 档 **+2.74%/笔（t +12.65）**，但其中 **51% 在 14:45 已封板买不进** → 必须另算"可成交子集"（+1.73%/笔、t +5.74）才能作为证据。**同族：[[#179-186 涨跌停不可成交]]；任何"追强势"的结论都要显式剔除封板/一字，并报剔除后的口径。**
"""
s2 = s2.replace(line0, line0 + "\n" + TRAP, 1)
p2.write_text(s2, encoding="utf-8")
print("project-engineering-traps.md：+4 条")

# ---------- 3) MEMORY.md 索引 ----------
p3 = MEM / "MEMORY.md"
s3 = p3.read_text(encoding="utf-8")
old = "**（R-g52negscreen-0919 反向筛接入三池立项"
new = ("**；**★★ R-topic293 gushi「两点半」判负期望（0919）：三条规则逐条负/零 —— F1 涨幅带 1~3% 全市场 21,890 笔次日开盘 "
       "−0.59%/胜率 17.7%（分年 2024→2026 单调恶化，次日收盘 2026 已 −1.30%）、F2「14:45 不回落」方向反（回落越多越好，"
       "通过 −0.452% vs 拒绝 −0.197%）、Top1 带内 corr −0.063 方向反；唯一正期望=平台真实名单 n=9（+2.72%/胜率 78%，不可采信）；"
       "F1 与 KHunter 超跌名单互斥（通过率 4%）⇒ 不可移植三池；**顺带真信号：14:30 涨幅 ≥9.5% 且 14:45 未封板 → 次日开盘 "
       "+1.73%/笔胜率 54.2%（t+5.74，封板 51% 已剔除）→ 建议交打板线另立预注册**；报告 `backtest/报告-topic293两点半滤网正负期望-20260919.md`，"
       "陷阱 +4 条（名单代理先验通过率 / 全天信息前视 / 求值器 O(n²) / 强势档先过可成交性）**；"
       "（R-g52negscreen-0919 反向筛接入三池立项")
if s3.count(old) == 1:
    s3 = s3.replace(old, new, 1)
    p3.write_text(s3, encoding="utf-8")
    print("MEMORY.md 索引已更新")
else:
    print(f"WARN MEMORY.md 锚点命中 {s3.count(old)}，未改（原句：{old[:30]}）")
