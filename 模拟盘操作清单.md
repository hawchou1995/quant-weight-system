# 三系统模拟盘 · 统一操作清单

> 入口文档（2026-09-14 建立）。全部模拟盘状态文件都在 `backtest/`，随 daily_refresh 链每日收盘自动推进；
> 本文件用于手动补跑 / 排查 / 口径核对。

## 一览

| 系统 | 口径 | 状态文件 | 手动跑 | 频率/触发 |
|---|---|---|---|---|
| **双卫星盘** | 轨A 冷门低波 Top10（34000）+ 轨B SUPER Top20（34000） | `backtest/satellite_paper.json`（+ `satellite_paper_init.json` 建仓目标快照） | `python backtest/satellite_paper_0914.py` | 每日 mark；轨A 60 交易日 / 轨B 月频调仓 |
| **KHunter 短线盘** | A 轨：熊市 RSI>59 出场；C 轨：RSI>50 激进版 | `khunter_paper_state.json` / `khunter_paper_state_c.json` | `python khunter_paper_20260903.py`（A）<br>`python khunter_paper_20260903.py --state khunter_paper_state_c --rsi-sell 50`（C，**--state 不带 .json**） | 每日 |
| **A5 打板盘** | 生产 P0 低开打板（昨日涨停∧gap(-5%,-2%)∧rp≤0.5 → 开盘买 → tp_t2） | `backtest/a5_paper_state.json` | `python backtest/a5_paper_0914.py` | 每日 |
| turn 影子轨（零资金） | 冷门低波 turn 观察 | `backtest/turn_shadow_state.json` | `python backtest/turn_shadow_0913.py` | 每日 |
| KHunter 看板快照 | A/C 双轨 → 看板 | `dist/khunter_paper_snapshot.json` | `python khunter_paper_snapshot.py` | 每日（看板重建前） |

## 自动链（唯一常跑入口）

- **每交易日 15:30**：automation-999fea3b → `python daily_refresh.py`
  （数据更新 → 短池/FB3 基金池 → 复盘 → 双卫星池 → 三轨信号 → **KHunter A/C → A5 → 双卫星盘 → KHunter 快照** → 看板重建 → gh-pages 部署 → main 同步）
- 非交易日自动跳过（闸门：`index_000300.csv` 最新日期 ≠ 今天）。
- 手动整链：`python daily_refresh.py [--skip-deploy] [--force]`。
- **警告**：`--force` 在盘中跑会让信号链用未收盘数据重算池与信号，禁止；补跑只用单系统脚本。

## 成本模型（各系统建档口径）

| 系统 | 佣金 | 税 | 滑点 | 备注 |
|---|---|---|---|---|
| 双卫星盘 | 2.5bp（最低 5 元） | 卖出 10bp | 20bp/边 | 见 `satellite_paper.json meta`；项目回测口径印花税为 5bp——如需对齐改脚本常量 `TAX` |
| KHunter | 0.575% 往返含费 | — | 含 | 与 KHunter 回测一致 |
| A5 打板 | 往返成本 1.15% | — | 含 | 与 A5 回测一致 |

## 口径注意（踩坑记录）

1. **建仓日 = 信号日（池 asof）的下一交易日开盘**。双卫星盘建仓目标冻结在 `satellite_paper_init.json`——否则 daily_refresh 重建池会改写 `satellite_pool.json`，导致建仓清单漂移。
2. `satellite_pool.json` 的 `rows[].lot` 是 **close×100 的展示值，不是股数**；模拟盘按"每只等分金额 + 整百股"自算。
3. A5 打板盘用"当日开盘"近似 T+1 开盘执行（脚本内有注释）；pending 出场用日期索引，跳日重跑不会错位。
4. 卫星盘 `nav_history` 首行在建仓次日起才追加（建仓前不记净值行）。
5. KHunter 双轨 nav 历史 09-03/09-04 起连续无缺口；如遇脚本漏跑，直接补跑当日即可（按日期索引，不依赖连跑）。

## 状态文件汇总（git 已跟踪）

`khunter_paper_state.json` · `khunter_paper_state_c.json` · `backtest/a5_paper_state.json` · `backtest/satellite_paper.json` · `backtest/satellite_paper_init.json` · `backtest/turn_shadow_state.json`（随 daily_refresh 的 git add 列表提交）
