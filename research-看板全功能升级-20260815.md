# research-看板全功能升级-20260815

> 日期：2026-08-15 21:20 ｜ 关联：`research-v9-auto普适信号体系-20260815.md`、`research-v8-lite个人投资者版-20260815.md`

## 一、需求（用户 5 连，全部落地）

1. 两个看板左侧贴边导航（点击跳转）
2. 搜索 / 筛选 / 排序 + 创业板/科创板权限切换（向下兼容）
3. 标的表区分板块、行业、当日涨跌幅、指标拆分、档位变化列
4. 逐标的详情看板（与当前系统适配）
5. 顶部导航：历史报告、明暗切换、qingju.me（青橘社区）保留

## 二、交付物（全部离线可用，无 CDN 依赖）

| 文件 | 说明 |
|---|---|
| `dual_system.html` | 双体系看板全功能版（87KB，数据另存） |
| `index.html` | 五 tab 主看板注入升级版（2.4MB） |
| `enhanced_data.js` | 共享数据源 `window.ENH`：34 只标的详情 + 2 体系 KPI/净值 + 132 篇报告索引（286KB） |
| `ui_components.py` | 公共 UI 层：主题 CSS（明暗变量）/ 顶部导航 / 左侧导航 / 表格交互 JS / 详情弹层 JS |
| `build_dual_system.py` / `build_enhanced_data.py` / `inject_index.py` | 三个生成脚本（数据更新后重跑刷新） |

**架构关键点**：
- `file://` 下 fetch 被 CORS 拦截 → 数据以 `enhanced_data.js`（`window.ENH = {...}`）+ `<script src>` 引用（本地相对路径可加载）
- index.html 不动模板 JS：MutationObserver 观察动态渲染的 `.trades-table`，注入排序/行点击；左侧导航点击触发模板 `.tab[data-tab]` 的 click
- 详情弹层：K线（250 日蜡烛，红涨绿跌）+ 四因子拆分条（动量35/趋势25/Aroon20/量价20）+ 权重分/动量历史曲线 + 两体系交易史（v9-auto/v8-lite 双 tab）
- 权限切换：主板(新开户)/+创业板/+科创板/ETF 按钮组，前端过滤（信号层始终全量计算，只影响展示——向下兼容）

## 三、验证

- dual_system：headless 渲染视觉通过（顶部导航/左侧导航/34 行标的表/搜索框/权限按钮）+ DOM 检查 + JS 语法（node --check 双份）
- index.html：视觉通过（左侧 5 tab 导航/主图 KPI 正常/布局无错乱）
- 详情弹层：自动触发 `openDetail('000636')` → DOM 确认风华高科弹层完整渲染（K线标题/因子条/交易史组件 8 处命中）
- 推送 GitHub：`33955f30`

## 四、已知边界

- 34 只标的 = 自选池 24 + v9 自动池 Top10（交集去重）；560390 等 1 只自选池标的数据长度不足未入
- 行业：自选池用 v6 模板行业口径，v9 Top10 用 westock profile（申万口径）——两类行业名不完全统一
- 历史报告 132 篇全部列出（滚动下拉），file:// 链接指向 Obsidian 本地目录（公司电脑需同步 Obsidian）
- 详情 K线/因子历史为 250 日抽样（60/40 点），非全量精度
