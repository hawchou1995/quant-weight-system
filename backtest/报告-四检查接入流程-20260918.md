# 报告 · 四检查接入流程（R-gatewiring-0918）

> 执行：2026-09-18 23:40 · 规格 `PRE-REGISTRATION_20260918_gate_wiring.md`
> 性质：流程接线 + 文档规程，n_trials = 0
> 用户批准原话：「第 1 项 + 第 2 项组合：机器自动拦 + 文档规定动作，双保险」

## 一、一句话结论

**双保险已落地，四项验收全过。** 检查现在**真的会拦住**（W2 实测：含 `inf` 的状态文件被判 FAIL
并报出精确路径 `/nav`、`/positions[0]/ret`，退出码 1），且**零删除**地接进了日链与项目规章。

| # | 验收 | 实测 | 判 |
|---|---|---|---|
| W1 | 日链闸空跑 | 6 个状态文件全 PASS，退出码 0 | **PASS** |
| W2 | **喂污染文件** | **FAIL + 退出码 1**，精确定位 `inf`/`nan` | **PASS** |
| W3 | 日链接线 | STEPS 31→32，新步第 30 位（看板重建之前），带 `[软]` | **PASS** |
| W4 | AGENTS.md 规章 | **removed = 0**，added = 21 | **PASS** |

## 二、机器自动拦（第 1 项）

新增 `backtest/factor_gate_daily.py`，**只跑便宜且高信号的两项**：
- ④ `warmup-check` 于面板 mask（~10MB，轻量）
- ② `finite-check` 于 **6 个日链状态 JSON** 的数字叶子（递归扫 `inf`/`NaN`）

接入 `daily_refresh.py` STEPS **第 30 位**——刻意放在**「看板重建」之前**，让 FAIL 在部署前可见。
`[软]` 语义：FAIL 只告警不阻断链，避免误伤日更。

## 三、文档规定动作（第 2 项）

`AGENTS.md` 新增**§四 · 因子立项前置检查**：四检查命令 + 五条纪律（① FAIL 即停不看收益；
③ 必须跑分位与 `--abs` 两种口径；「不可判定」≠「未过门」；④ WARN 走裸面板版；四检查只能证伪不能证明），
并写明**这一节为什么存在**（0918 四类缺陷的教训）。

## 四、实施中抓到的两处自身缺陷

1. **路径解析错**：`p = BASE / rel` 使 `backtest/xxx.json` 变成 `BASE/backtest/backtest/xxx.json`
   → **受检文件 0 个却报 PASS**（静默空跑）。这是项目**既有陷阱「相对路径 IO 静默落空」的又一次复现**——
   陷阱早在库里，我仍然踩了。
2. **W2 首跑假阴性**：正因为上面的路径错，含 `inf` 的测试文件根本未被加载 → 报 PASS + 退出码 0。
   **若无 W2 这条验收，这个缺陷会直接上生产并继续静默。**

## 五、改动的生产文件（已备份）

| 文件 | 改动 | 备份 |
|---|---|---|
| `daily_refresh.py` | STEPS +1 步（31→32） | `D:/Tools/cache/mem_bak_0918/daily_refresh_pre_gate.py` |
| `AGENTS.md` | +1 节（零删除） | `D:/Tools/cache/mem_bak_0918/AGENTS_pre_gate.md` |

## 六、边界

日链步骤**只覆盖产出物**，**不覆盖"新写的因子脚本"**——那部分靠 AGENTS.md 规程靠人执行。
`warmup-check` 只能检出域门存在，不能判断该门是否正确。

## 七、产物

`backtest/factor_gate_daily.py` / `daily_refresh.py`（+1 步）/ `AGENTS.md`（+1 节）/
`_tmp_gate_bad.json`（测试夹具）/ 规格（结果已回填）
