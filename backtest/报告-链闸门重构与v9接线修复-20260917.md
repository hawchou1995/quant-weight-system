# 报告 · 链闸门重构 + v9 接线修复 + index_000300.csv 坏行治理 — R-gate-0917

> 日期：2026-09-17｜状态：**已上线**（gh-pages `016cd50`；main 见提交）｜授权：用户「全部改，改到位」
> 背景：卡死链处置复核（09-16 傍晚已 kill+修+重跑完成，双账户建仓 8/18 与预测零偏差）中发现
> 「15:30 自动链恒 [skip]」的闸门缺陷与「tbl-v9 退役致短线两表联动静默失效」的既有 bug。

---

## 一、改了什么（三件 + 一件附带）

### ① 交易日闸门重构（`daily_refresh.py`）——根治"自动链从不自动跑"

**旧逻辑**：以 `index_000300.csv 末行 == 今天` 判交易日。但该文件**仓库内无写手**（长期靠人工"补 HS300 指数行"），
15:30 时末行必为昨天 → 自动链恒输出 `[skip] 非交易日` → **实际全靠傍晚人工 `--force` 追跑**
（09-16 卡死进程命令行带 `--force` 即为此模式的现场证据）。

**新逻辑**：
| 环节 | 行为 |
|---|---|
| 周末 | 直接跳过（周六/周日，保留 `--force` 旁路） |
| 工作日 | **先跑数据步**（update_daily），再由新步骤 `ensure_index_row` 判定 |
| 判定 | 今日 HS300 行情**可取得** = 交易日 → 自动**写入/刷新** `index_000300.csv` 末行（消除人工补行 + 修正 review_daily 等步骤的 as-of 依赖）→ 继续全链 |
| 不可取得 | 节假日/数据源未就绪 → 该步 **exit 3**（约定码）→ 主链打印 skip 并**干净退出**（数据步已完成，无副作用） |
| `--force` | 旁路闸门（exit 3 仅告警继续） |

**新模块** `backtest/ensure_index_row.py`：主源=东财 kline（对 09-16 行逐位对拍 ✓）／备源=新浪 hq 快照
（东财抖动时实测自动顶上 ✓）；**幂等**——盘中写入的非终值行，收盘后重跑自动**就地刷新**；
volume 口径=股（与库内主流 2833 行一致）；行尾从**字节**判定并保持（CRLF）。

### ② v9 接线中断修复（`build_dual_system.py`）

JS 联动接线循环 `['tbl-v9','tbl-short-stk','tbl-short-fund']` 中 `tbl-v9` 已于 09-13 退役移除
→ 首个 id 即 `table=null` 抛错（缺空值保护）→ **整个 forEach 中止** → 短线两张**在用**表格
（股票池/基金池）四类联动静默失效。修法：列表**移除 v9** + 加 **`if(!table)return;`** 空值守卫。

### ③ index_000300.csv 坏行治理（附带的审计发现，已修）

接管该文件维护后做全表对拍（东财分页全量 2015+ 与新浪 1500 根**两份独立源交叉印证**），发现 **15 个坏行**：

| 类型 | 行 | 修法 |
|---|---|---|
| volume 误写"成交额" | 2026-08-18 / 08-19 / 08-20 / 08-31 | 换算为股（旧值比值 21.9~100×） |
| volume 用了"手" | 2026-08-24、09-01/02/03/04/07/09/10/14（9 行） | ×100 → 股 |
| OHLC 错误 | 2026-08-19（low / close） | 按双源一致值修正（close 4604.0→4588.696） |
| volume 舍入 | 2026-09-15 / 09-16 | 归一到源精确值（差 <1e-6 相对量级） |

修正后复验：**待修 0 行**；`git diff` 恰为 15 行（曾因 `read_text` 吞 CRLF 误改全文件行尾，已复原并修脚本）。

### ④ 自动化：无改动
`automation-999fea3b`（每交易日 15:30）命令与提示词**无需改**——之前是"命令对、脚本闸门错"，现脚本已修。

---

## 二、验证证据

| 项 | 证据 |
|---|---|
| 闸门放行 | `ensure_index_row --check`（真实文件）exit 0「可追加 2026-09-17 行」——**旧逻辑此刻必然 skip，新逻辑正确放行** |
| 写入路径 | 临时副本：append ✓ → 重跑幂等"已最新" ✓；东财断连时新浪备源顶上 ✓ |
| 非交易日路径 | 未来日期文件 → **exit 3** ✓（约定码） |
| CRLF | 追加后字节级验证 `\r\n` 保持 ✓ |
| v9 接线 | 渲染实测：控制台错误 **0**（修复前有 1 条 uncaught）；卡片过滤 `3→0` 存活 ✓；卡片计数文案更新 ✓；行 `cursor=pointer` ✓；点击行弹层 ✓；交互后错误仍 0 ✓ |
| 部署 | `_deploy_fundline_0911.py` RESULT: ALL PASS（md5 逐一对拍，`016cd50`）；线上回读：修复标记 2 处、旧列表 0 处 |

## 三、今晚（09-17 15:30）预期行为

`automation-999fea3b` 触发 → 工作日 ✓ → update_daily 抓 09-17 → **ensure_index_row 自动补 09-17 指数行**（首日生效）
→ 全链（含 kxmm 抓取）→ 看板部署 → main 同步。**不再需要人工 `--force` 追跑**。

**残余风险（如实申报）**：① 若 15:30 数据源尚未出齐（罕见），`update_daily` 与闸门都判"无今日数据"→ 链会 skip 且**当晚不会自动补跑**（需人工重跑一次）——若要自动重试可另拍板加"16:30 重试一次"；② 历史 2015~2020-07 段未逐行对拍新浪（东财全量对拍已覆盖=口径 1.0 占 2833 行，异常仅上述 15 行，均落在 2020-07 之后窗口，交叉印证一致）。

## 四、复现

```bash
cd D:/Documents/Workbuddy/股票基金/quant-weight-system
PY=C:/Users/Admin/.workbuddy/binaries/python/envs/default/Scripts/python.exe
$PY backtest/ensure_index_row.py --check     # 闸门体检（不写）
$PY _audit_index_rows_0917.py                # 指数行全表审计（dry；--fix 落盘）
$PY build_dual_system.py && $PY _deploy_fundline_0911.py
```
