# 报告 · 吸收外部回测专家规则库（R-mine-expert-0918）

> 来源：`D:/Documents/Workbuddy/专家/zcode-marketplace/plugins/strategy-backtest-expert/skills/quant-backtest-lab/reference/`
> 目标：与本项目陷阱库交叉比对，只抄**真有增量**的
> 性质：规则吸收 + 陷阱入库；**未改动插件任何文件**

## 一、结论

**插件规则库的颗粒度整体低于我们**（它停在"要申报/要披露"，我们在"要量化/要断言"），
但它有**一整块我们确实空白的东西**：**单笔成交层面的微观语义**。

> 一句话：**我们不是缺规则，是缺"单笔成交层面"的显微镜**——沉淀全在组合级/统计级，
> 而它的价值全在"一笔成交到底按哪个价、哪一天、能不能成交"。

## 二、交叉比对结果（已核验）

代理首轮声称 14 条值得抄，其中 4 条属"改数字"级别。**我逐词 grep 复核了它的"零命中"声明**：

| 关键词 | 实测 | 与代理说法 |
|---|---|---|
| `移动止损` `跟踪止损` `最高价` `跳空` | **均 0** | 一致 ✓ |
| `末仓` `未平仓` `强制平仓` | **均 0** | 一致 ✓ |
| `Adj` `港股` `印花税` `抢占` `R倍` `点事件` | **均 0** | 一致 ✓ |
| `一字板` | 0 | 一致 ✓ |
| `涨跌停` | 1 | 代理**主动披露**"仅 1 处且是 #131 抽象原则" ✓ |
| `金叉` | 1 | 代理**主动披露**是 TDX 分词语境 ✓ |
| `网格` | 8 | 代理说 9 处，**数目小差**（且它指出的"全是相位/参数网格"结论正确） |
| `优先级` | 4 | **代理未披露**（它称"无此类条目"）—— 唯一一处轻微失准 |

⇒ **代理核心声明成立**，且对多数反例主动披露。**这是可信的。**

## 三、已入库条目（★187–194，共 8 条）

| 新号 | 主题 | 级别 |
|---|---|---|
| **★187** | **单笔成交层面的微观语义**：止损止盈用 `low/high` 非 `close`、同 bar 止损优先、跳空按 `open`、移动止损最高价**后**更新、台账按**成交时点**落笔、末仓显式处理、止损基准=入场价、**R 倍数是绝对价差不是百分比** | 🔴 改数字 |
| **★188** | 涨跌停/一字板**不可成交** + 网格跨档**不可压缩成一笔** | 🔴 改数字 |
| **★189** | **Warmup 门控必须分层**：只档交易副作用，连板计数/入场后最高价/状态机**必须继续更新** | 🔴 改语义 |
| **★190** | **点事件 vs 状态**（`ma5>ma10` 是状态、每天重复出信号）+ 解析期三条（不静默改写 `>`→`>=`、今天/昨天显式化、事件生命周期） | 🟠 改信号集 |
| **★191** | **导出层断言范式**（4 个 `_enforce_*`）——把"净值动了但成交为空"从事后对账变成**跑不出文件** | 🟠 工程化 |
| **★192** | **跨市场规则块：港股 / 美股**（board lot 按票、碎股、T+2 可回转、long-only 无硬闸、印花税双边、午休+CAS；美股 **绝不混用 raw OHLC 与 Adj Close**、不套 T+1） | 🟠 防未来 |
| **★193** | **美股数据源实测三雷**：**新浪 TQQQ 未复权 → 8 个假暴跌日（−49%~−67%）**、Yahoo `close` 是拆股后复权、VIX 主源用 CBOE；另 FRED 假期空值 / 系统代理致 akshare ProxyError / urllib 打 FRED 会 RST | 🔴 改数字 |
| **★194** | **多策略交互语义必须冻结**（优先级冲突三选一、卖出 A 腿当天能否买 B 腿、调仓频率的作用域） | 🟠 防分叉 |

## 四、明确**不抄**的（如实说）

| 条目 | 判定 |
|---|---|
| "股票池不得被静默改写" + 幸存者偏差披露 | **正确的废话**——我们已做到实现层（★名单过滤与生产同源 / #172 宇宙守护 / #86 覆盖缺口分档），它只要求"披露" |
| "未明说参数不得静默假设" | **正确的废话**——我们 ★「拍板值必须与执行值同源」+ #146 结论边界是**实现级**版本 |
| "数据覆盖不全须申报 M/N" | **已被我们超越**（门控分母分档 + 退市/长停识别 + ABORT 阈值） |
| "信号时点 ≠ 执行时点" | **复述**（#34 已有实锤） |
| "打分制不能塌缩成 AND/OR" | **方向对但太软**——我们会写成"区间/上限型约束不能用连续惩罚+排序表达"（带识别特征 `rank(score−pen×1e-6)`） |
| "参数化费率 / 不要硬编码" | **零信息量** |
| 各节末尾的 "must be disclosed" 系列 | 抄规则，别抄这句 |
| `SKILL.md` 整体（47.9KB）与看板渲染栈（177KB） | **不建议整抄**——前者是通用流程手册，后者与 `build_dual_system.py` **平行实现**，抄了会分裂 |

## 五、并发会话冲突（如实上报）

本记忆库**有另一个会话在并行写入**，已发现：
1. `project-us-etf-strategy.md`（00918 立项）**不是我建的**
2. `project-report-readability.md` **不是我建的**
3. 陷阱库中 `★176`（tool-call 字符丢失）与 `★178`（好到不可能是真的）**也不是我写的**——与我的 ★176/★178 **撞号**
4. 我的新条目**已改为 ★187–194 避让**；**未改动对方任何条目**
5. 相对 178 备份**有 1 行丢失**（`nav_history` 那条 blockquote）——该主题**仍在库中 5 处**，判断为并发会话去重所致，**未恢复**

**建议**：若确实在跑两个会话写同一份记忆，应约定分工，否则会持续出现撞号与互相覆盖。

## 六、产物

- 陷阱库新增 **★187–194**（8 条，含核验说明与"不抄"清单）
- 本报告；插件本体**零改动**
