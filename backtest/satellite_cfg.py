# -*- coding: utf-8 -*-
"""双卫星配额的**唯一来源**（R-single-track-0915）。

教训：0914 拍板把配额改成 25/75，但只更新了 satellite_paper.json 的 meta，
     未传导到建仓执行 → 实盘按旧配额（各 34k）建仓，轨B 只买到 16/20 只，
     且连续两轮分析建立在错口径上。**故配额必须单一来源、执行时读取。**

拍板（2026-09-15）：实盘只买一套 → **单跑轨B（68,000）**；轨A 转**零资金对照轨**。
"""
CAP_A = 0            # 轨A：零实盘资金（仅记录信号与幻影净值）
CAP_B = 68000        # 轨B：主轨，独享双卫星全部资金
NAV_A = 17000        # 对照轨的**名义基数**（仅用于计算幻影净值，不代表任何实盘资金）
ROLE_A = "对照轨·零实盘资金"
ROLE_B = "主轨"
REBASED = "2026-09-15"
