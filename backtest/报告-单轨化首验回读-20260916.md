# 报告 · 单轨化后首次收盘链回读核查（2026-09-16 16:40）

> 任务：`automation-12fa9cb6`（15:30 自动链首验回读）。**只读核查，未修改任何生产文件、未补单、未干预进程。**

## 一、结论（先说结果）

| 项 | 结论 |
|---|---|
| **链是否完成** | ❌ **未完成——死锁在第 1 步「数据更新」内部的复权检测阶段**（已 44 分钟，CPU 零增长） |
| **双账户建仓** | ❌ **未发生**（两账户仍为空仓） |
| **即使链恢复也不会建仓** | ⚠️ **是**——本轮链的数据目标日是 **09-15**（今天 09-16 的行情尚未入库），而建仓需要 09-16 开盘价 |
| 看板重建 / 部署 / git 提交 | ❌ 均未发生（看板时间戳仍 09-15 21:51；无当日 chore(daily) 提交） |

## 二、链的实时状态与证据

**时间线**：

| 时刻 | 事件 | 证据 |
|---|---|---|
| 15:51 | 15:30 自动链的会话启动（延迟约 22 分钟） | MCP 进程批量创建 15:51:11–15:51:14 |
| 15:54:50 | 执行 `daily_refresh.py --force`（PID 38976，bash 43564） | 进程树 |
| 15:55:04 / 15:55:18 | **数据更新阶段完成**（滞后清单 409 只 → 更新 431 文件，fail 清单为空） | `data_lag_list.csv` 15:55:04（409 行）、`data_full_fail_list.csv` 15:55:18（仅表头=无失败） |
| ~15:55:20 起 | 进入 `run_rebase_check`（复权基准漂移检测）→ **卡死** | 之后无任何产物写入 |
| 16:31 / 16:38 | **CPU 时间完全冻结**（971875000 → 971875000，100ns 单位）、WS 168MB 不变 | 两次采样间隔 7 分钟零增长 |

**进程树（确认只有一条链，无并发）**：
```
bash(43564) → daily_refresh --force(38976) → daily_refresh(27604) → update_daily(6004) → update_daily 工作进程(8036, WS168MB, CPU 冻结)
```

**卡点定位（代码级）**：`update_daily.py` 在数据更新完成后依次执行 `_flush_index()` → `run_rebase_check()`；后者第一步用 **akshare 通道**做新浪探针/漂移验证：
```python
fetcher = F.fetch_sina_daily   # akshare：股票接口，内部 requests 不带 timeout + 每次新建 py_mini_racer VM
probe_date(fetcher)  # 60s 闸
```
现有兜底机制 = 「单候选 90s 上限（`_run_with_deadline` daemon 线程 + join）+ 相位 1500s 预算」。**但该机制在本类卡死下失效**：若卡点是持有 GIL 的 native 死锁（py_mini_racer/V8），主线程连 `join` 的超时判断都拿不到 GIL，闸门形同虚设——与观测到的 CPU 0% 死等完全一致。

**关键旁证（同族问题第三次复发）**：`update_daily.py:375` 注释自述——
> 「复权检测的取数通道（2026-09-15 修复『无超时阻塞』）：**0914 卡 52min / 0915 卡 76min**。根因：run_rebase_check 用 F.fetch_sina_daily → akshare stock_zh_a_daily，其内部 requests 不带 timeout（且每次新建 py_mini_racer VM）→ 单个候选把整段挂死且无法自愈。」

今日为**同类卡死第 3 次**（44min+ 且仍在卡）。另一旁证：16:35:50 有并行活动在诊断 `py_mini_racer` 常驻线程（`Thread-1 (run_event_loop)` daemon=True）——与上述根因同向。

## 三、双账户实测（未建仓状态，结构正确）

| 项 | 轨A（对照轨） | 轨B（主轨） | 判定 |
|---|---|---|---|
| `meta.basis` | **17000** ✓ | **68000** ✓ | 与 `satellite_cfg`（NAV_A / CAP_B）一致 |
| 顶层 `cash` | 17000.0 ✓ | 68000.0 ✓ | 未动用 |
| `positions` / `fills` / `nav_history` | 0 / 0 / 0 | 0 / 0 / 0 | **空仓待建仓**（符合「信号日 09-15 → 次一交易日 09-16 开盘建仓」的时序） |
| `meta.role` | 对照轨·零实盘资金 ✓ | 主轨 ✓ | |
| 冻结快照 | `satellite_paper_init.json` asof=**2026-09-15**，轨B 20 只 / 轨A 10 只 ✓ | | |

**观察项（非阻塞，供留意）**：两文件的 `meta.initial_cash` 均为 **68000**（遗留值；A 文件的真实基数是 `basis=17000`）。当前消费方均安全（`build_dual_system.py:127` 优先读 `basis`；`satellite_paper_0914.py` 用 `BASIS` 常量算各轨净值），且该脚本下次写回时会把两文件的 `initial_cash` 统一改为 **85000（合计口径）**——语义易混淆，建议加注释或在 v1.3 清理。**本次未改。**

## 四、`--force` 与数据目标日（重要）

- 链以 `--force` 运行（绕过 `expected_trade_date` 滞后检查）：15:54 启动时数据源最新为 **09-15**（今天 09-16 的日线尚未发布），故本轮链的目标是**09-15 的数据补录**（滞后 409 只 → 已补 431 文件）。
- **`satellite_paper` 建仓判据**：`auto_dates = [d for d in cal if d > asof(09-15)]` → 当前数据无 09-16 日期 → `fill_date=None` → 会打印「待建仓但数据未到」并跳过。
- ⇒ **本轮的"首验点"性质需修正**：15:30 链在当天行情未发布时无法完成建仓；**建仓要等 09-16 数据入库后的重跑**（与 09-15 的惯例一致：cb32502「数据齐全后重跑复盘」）。

## 五、建议（未执行，待决策）

1. **短期**：视链卡死取舍——(a) 终止 `update_daily`（PID 8036）让链走失败路径中止，数据齐后（一般 17:00+）重跑 `daily_refresh.py`；(b) 继续等（历史同族 52/76 分钟后的事后处置是改代码，未见「自愈」证据，且本机 CPU 0% 指征死锁）。
2. **中期（根因修，需另行拍板）**：
   - 把 `run_rebase_check` 移出主进程——**独立子进程 + 进程级硬超时 kill**（线程闸无法对抗持有 GIL 的 native 死锁）；
   - 或恢复「fast_sina_fetch 直连（≥10s timeout）做候选剪枝」，仅对确认漂移的候选走 akshare 全量重拉（保口径、去卡点）；
   - 或为 akshare 通道显式设置全局 socket 超时（`socket.setdefaulttimeout`）作为最后防线。
3. **建仓路径**：数据齐后重跑链即自动完成（轨B ≈18/20、轨A ≈8/10 可买，见 `报告-双账户建仓回读-20260916.md` 的预期，以及 §三 结构校验）；建议重跑前确认 `update_daily` 不再卡（否则用 `--skip-data` 跳过数据步，数据另行补给）。
4. **流程**：建议把「rebased/首验」类预期写成"数据齐全后重跑"而非固定 15:30，避免再次以"当日 15:30 自动链"作为建仓验收点。

## 六、附：本次核查执行的命令（可复现）

```bash
# 进程与活性
powershell -NoProfile -Command 'Get-CimInstance Win32_Process | ? {$_.Name -eq "python.exe"} | Select ProcessId,CreationDate,CommandLine'
# CPU 冻结判定（两次采样）
# 产物 mtime 全景
find . -maxdepth 2 -newermt "2026-09-16 15:40" -type f
# 账户与配置
python -c "import json; ..."  # satellite_paper_{a,b}.json / satellite_paper_init.json / satellite_cfg.py 对拍
```

**纪律声明**：全程只读；未修改任何生产文件；未重跑任何脚本；未 kill 任何进程；未补单。
