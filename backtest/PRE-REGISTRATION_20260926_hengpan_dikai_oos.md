# PRE-REGISTRATION｜横盘放量·次日低开·隔日尾盘卖 — 前向 OOS 验证

- 预注册日期：**2026-09-26**
- 状态：**已批准，已启动**（用户于 2026-09-26 批准）
- 冻结脚本：`backtest/hengpan_fangliang_dikai_0925/oos_run.py`
- 脚本 SHA256：`53040b9c9c225a1d572273fe5024775d824c93320eb260dee9dd9ce1dc29a6fe`（大小 8,217 B）
- 状态文件：`oos_state.json`（`shadow_start` / `last_scan_date` / 哈希）｜ 交易台账：`oos_trades.jsonl`｜ 验收报告：`oos_report.json`
- 样本内依据：`报告-横盘放量次日低开-优化方向研究-20260926.md`（含 §11 投产判定、§12 幸存者偏差修复）

---

## 0. 为什么需要这份预注册

样本内结果（复合打分、年化 +127.91%、净胜率 56.11%、0/11 负年）是**在 13 个排序键搜索 + 参数网格之后**得到的，存在配置窥探（config-snooping）风险。**唯有冻结规则、在样本外前向记录，才能证明它不是搜索出来的。**

本文件把「判据、参数、执行假设、终止条件」一次冻结。**批准后不得修改任何一条。**

---

## 1. 冻结策略规格（逐条、无歧义）

**信号日 T（收盘后判定）**

1. 股票资格：`有效价(close>0)` 且 `上市有效交易日 ≥ 250` 且 `close[T] ≥ 2.00 元` 且 `AMT20[T] ≥ 2,000 万元`
   - `AMT20` = 过去 20 个有效日 `amount` 的均值
2. 次日跳空条件（在 T+1 开盘时判定）：`gap = open[T+1] / close[T] − 1 ∈ [−3.00%, −1.00%]`
   - 即：**低开至少 1%，但不得低开超过 3%**
3. 复合打分（在**当日全部满足 1+2 的候选**上做横截面标准化）：
   ```
   F = z(−ln AMT20) + z(−ln VOLBR) + z(−RET20)
   VOLBR = volume[T] / VMA20_prev[T]      # VMA20_prev = 过去 20 有效日均量
   RET20 = close[T] / close[T−20] − 1
   z(x) = (x − mean) / std               # 当日候选池内
   ```
4. 选股：按 `F` 降序取**前 10 只**（每日最多 10 只，不足则全买）
5. 仓位：每只 `= min(前一日净值 / 20, 可用现金)`；最多同时持有 20 只；**同一标的持仓期内不重复买入**

**交易**

6. 买入：**T+1 开盘价**（集合竞价，单一价格撮合）
7. 卖出：**T+2 收盘价**（尾盘）
8. 出场价缺失（停牌）：顺延至下一个有效价；若 5 个交易日内无有效价，则取最近有效价；仍无则按 0 计（清零）
9. 成本：**双边 6.92 bp 往返** = 佣金 万0.86×2 (1.72bp) + 印花税 5bp（卖出）+ 过户费 0.1bp×2 (0.20bp)
10. 净值：逐日收盘盯市

---

## 2. 冻结参数

| 参数 | 值 | 说明 |
|---|---|---|
| P1（低开下限） | 0.01 | 低开 ≥ 1% |
| P2（低开上限） | 0.03 | 低开 ≤ 3% |
| K（每日选股数） | 10 | |
| KSLOT（最大持仓） | 20 | |
| MINAMT | 2×10⁷ | 20 日均额下限 |
| MINPX | 2.00 | 最低股价 |
| LISTED | 250 | 最少有效交易日 |
| COST_SIDE | 0.000346 | 单边 3.46bp |
| SHADOW_START | **2026-09-25** | 样本外起点（样本内止于 2026-09-24） |

**股票池**：现存 5,207 只 + **退市股 253 只**（`backtest/_delisted_universe/delisted_bars.csv.gz`，腾讯 qfq 源）= 5,460 只。**含退市股是硬要求**（见 §12 幸存者偏差修复：不含时年化虚高 +15.2pp）。

---

## 3. 运行机制

```bash
python backtest/hengpan_fangliang_dikai_0925/oos_run.py
```

- 幂等、可追赶：每次运行从 `shadow_start` 扫到最新可用数据日，重复运行不会重复记录
- 无人工判断：规则 100% 确定性，故**允许回溯计算**；完整性由「脚本哈希 + 本文件冻结」保证
- `shadow_start` 之前的数据一律不计入 OOS
- 首批可评估时点：`≥300 笔` 约需 30 个信号日（约 2026-11 中旬）；`≥120 个信号日`（跨 ~6 个月、覆盖多市场状态）约需至 **2027-03 前后**
- **取二者较晚者 —— 即约 2027-03 前后才能给出「通过/不通过」结论**；此前只能报进度

**执行质量记录（用户每日手工填）**：`oos_trades.jsonl` 每行含 `real_fill_px` 与 `real_ret_pct` 字段。**请在 T+1 收盘后填入当天的实际成交均价**，判据 ② 依赖它。

---

## 4. 验收判据（冻结 —— 不得事后调整）

### ① 样本量门槛
- **≥ 120 个信号日** 且 **≥ 300 笔已完成交易**
- 未达门槛前，不得给出「通过/不通过」结论（只能报进度）

### ② 执行判据（唯一真正的成败线）
- `实际成交均价 ÷ 当日开盘价 − 1 ≤ +0.20%`（按笔加总后再平均）
- 依据：样本内滑价阶梯 —— +0.2% 滑价下年化 **+90.80%**（四闸 4/4）；+0.5% 滑价下仅 **+34.61%**；+1.0% 滑价下 **−24.37%**

### ③ 四闸（用**实际成交价 + 6.92bp 成本**重算）
| 闸 | 阈值 |
|---|---|
| 单笔净均值 | > 0 |
| 净胜率 | ≥ 46% |
| 单笔净中位 | > 0 |
| 净超额（对沪深300，同起点同口径） | > 0 |

### ④ 风控判据
- `−3%` 下界必须被执行：被过滤笔数占比应落在 **10%~22%**（样本内基准 16.21%）
- 若实际占比 < 5%（说明没在执行过滤）→ 本次 OOS **作废重来**，不计入结论

### ⑤ 判定
- **全部通过** → 可进入实盘小仓位（≤ 容量上限的 1/3）
- **任一不过** → **放弃该策略**，回到样本内不再调参（禁止在同一数据上继续搜索）

---

## 5. 终止与禁止事项

**禁止**（违反即本次 OOS 作废）：
1. 修改 `oos_run.py` 的任何判据/参数（修改后 SHA 变化 → 必须重新预注册 + 重启 OOS）
2. 在 OOS 样本上再做排序键搜索、参数网格、或任何形式的择优
3. 事后再调整判据阈值、剔除「异常」样本、或改变成本假设
4. 用不同成本/滑价口径混报结论

**允许**（属数据维护，不属规则修改）：
1. 重新抓取退市股名录与行情（`_delisted_universe/`），用于补齐新退市标的
2. 补齐缺失的历史日线（同一标的重抓）
3. 修复数据错误（须记录在 `oos_state.json` 的 `data_fixes` 字段）

---

## 6. 已知局限（不因预注册而消失）

1. **容量上限 ≈ 200~300 万元**（集合竞价深度口径）；单一账户规模不可超过
2. **93.5% 的边在入场日盘中** → 完全依赖开盘成交质量
3. **未建模**：竞价部分成交、涨跌停不可卖（样本内 0.41% 出场撞跌停）、ST 股（仅剔除了当前名含 ST 的）
4. **退市股价格为腾讯源**：与 `data_full` 的日收益平均差 2.17bp（价格取整噪声量级），已做双源交叉校验
5. **T+1 制度**：本策略持有 T+1 开盘 → T+2 收盘，跨一个完整交易日，不受 T+1 卖出限制影响
6. **历史 ≠ 未来**：小微盘结构性效应可能随拥挤度上升而衰减（样本内 2024 年净均已降至 +0.74% 附近）

---

## 7. 批准记录

- **2026-09-26 用户批准**：「批准前向OOS预注册」+「幸存者偏差修复」
- 幸存者偏差修复已完成并纳入本预注册的股票池定义（§2）
- 本文件一经批准即为**不可变契约**；后续状态更新只写入 `oos_report.json`，不回改本文件
