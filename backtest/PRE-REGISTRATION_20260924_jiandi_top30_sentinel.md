# PRE-REGISTRATION · 见底共振 Top30 前向哨兵（R-jiandi-top30-0924）

> 上游：`jiandi_top_grid_0924_out/报告-见底共振×高点警示-网格回测-20260924.md`（§10 验证套件 / §11 敏感度与复格）
> 触发：候选口径「θ=30 + RSI 出场 + K=5」在 **1.15% 保守成本档**组合级 = CAGR 11.50% / Sharpe 0.921 / MDD −20.1%（回测；含选择偏差披露见 §三）→ 上生产前先建**只读只可否决**的前向哨兵。

---

## 〇、定位（先写死，防止被误用）

| 项 | 值 |
|---|---|
| **职责** | **只可否决**（触发人工复核）；**无**自动改参/回滚权；**不**构成通过性证据 |
| **冻结口径** | 入场＝K≥2 见底信号共振 且 **当日共振家数 θ≥30**；出场＝**RSI(14) 卖出阈值触发（次日开盘卖）**，兜底 5 日；执行＝T 收盘确认→T+1 开盘买；标的＝主板；K=5 等权；成本双档 0.2% / 1.15%（往返） |
| **三臂** | ①基线（纯持 5 日）②RSI 固定 55（主假设）③RSI 域阈值 55/65/65（次假设，网格内选出，仅对照） |
| 数据 | **只读** `v8_factor_cache.pkl`；哨兵自建纸面账本（**不写任何既有账本/参数**） |
| 运行 | 每交易日回读时运行（暂不并入日更链）：`python backtest/jiandi_top30_sentinel_0924.py`（`--dry` 只算不写；`--start` 重放验证） |
| 判定节奏 | **≥300 完成事件 或 ≥120 交易日**后首次正式判定；此前每日只**记录** |
| 起点 | shadow_start = 2026-09-24（注册日；此前数据只用于口径复现） |

## 一、哨兵指标（每交易日记录）

事件级：`n_events_done` / `mean_net_pct` / `wr_pct` / `med_pct`；组合级（K=5·seed20260921·两成本档）：`cagr` / `sharpe` / `mdd`；频率对照：触发日数（回测预期 ≈8.5/年）、事件数（≈954/年）。

## 二、否决规则（冻结；任一触发 → 人工复核）

| # | 触发条件 | 依据 |
|---|---|---|
| **V1** | 完成事件 ≥300 且 `mean_net_pct ≤ 0` | 事件级粗差（回测参照 +3.13%~+3.35%） |
| **V2** | 纸面组合 `mdd` ≤ **−30.2%**（= 回测 −20.1% × 1.5） | 回撤超回测 1.5 倍 |
| **V3** | ≥120 交易日 且 组合年化 ≤ **−10%** | 前向唯一可辨的是粗差 |
| **V4**（诊断，不设门） | top10 日收益集中度 ≥ **60%**（回测 42.1%） | 结构劣化提示 |

**触发处置**：写入 `jiandi_top30_sentinel_state.json` + 回读报告 + **不自动改任何参数**，由用户决定。

## 三、明确不做 / 选择偏差披露

1. 不能证明"优于基线"（前向样本/功率不足，仅方向性对照）；不做显著性检验；不自动动作。
2. **选择偏差**：「θ=30」「RSI 55/65」「K=5」均在同一样本上筛选（阈值网格 40 格 + RSI 网格 80 格 + 复格 48 格）——本哨兵只回答"是否发生粗差"，不回答"是否真实优于基线"。
3. RSI 周期 6/9/14 同量级（21 过慢）；分域判定标准不敏感（MA20–250 皆可，生产 MA60 在最优区）。

## 四、复现

```bat
python backtest/jiandi_top30_sentinel_0924.py --dry                 :: 重放（默认起点）不写状态
python backtest/jiandi_top30_sentinel_0924.py --start 2026-06-01 --dry  :: 验证重放（对照历史）
python backtest/jiandi_top30_sentinel_0924.py                       :: 正式运行（写状态）
```
产物：`backtest/jiandi_top30_sentinel_state.json`

---
> **状态更新（2026-09-24）**：用户已批准本预注册。哨兵脚本 `jiandi_top30_sentinel_0924.py` 已实现，并经①全历史重放（三臂与 §11D 冻结值逐位一致）②窗口重放（V1–V3 全 False）双验证；正式状态已初始化（`jiandi_top30_sentinel_state.json`，shadow_start=2026-09-24，status=COLLECTING）。验证详情与事件参照精确值见报告 §13。