# 报告 · 在产因子回溯前视审计（R-audit-0918）

> 执行：2026-09-18 18:30 · 协议冻结 `PRE-REGISTRATION_20260918_audit_prod.md`
> 性质：排查（非研究臂）——**未改动任何生产代码**，**n_trials = 0**

## 一、总结论

**在产因子未发现前视证据。** 7 项审计对象全部 PASS 或给出结构性安全证据。
当日唯一真实的前视命中位于 **R-tdx6 研究臂**（已勘误、已修、已隔离 `_buggy_lookahead_0918/`）。

| 组 | 因子 | 方法 | 结果 |
|---|---|---|---|
| A1 | A5 池B `ADX14` | audit，**直接调用生产函数对象** `paper_daban_a5._adx14`（列抽样 sub=6） | **PASS**（8 日抽样 0 不一致，max_diff 0.0） |
| A2 | A5 池A `ret20` | audit（全列，sub=1） | **PASS**（0/8，0.0） |
| A3 | A5 `rel_pos60` | audit（全列） | **PASS**（0/8，0.0） |
| A4 | A5 `dist_high` | audit（全列） | **PASS**（0/8，0.0） |
| B1 | 卫星池三低分 `score` | 代码级结构证据 | **结构性安全** |
| B2 | `pct40` 出场分位 | 代码级结构证据 | **结构性安全** |
| C1 | 短线 KHunter 信号 | 代码级结构证据 | **结构性安全** |

## 二、B/C 组的结构性安全证据（代码逐字）

### B1 卫星池三低分 —— 帧索引无时间维
```python
df_a = ln_df.set_index("code")
amt_pct  = 1 - df_a["amt20"].rank(pct=True)   # 低成交额=高分位
atr_pct  = 1 - df_a["atr20"].rank(pct=True)   # 低波动=高分位
turn_pct = 1 - df_a["turn20"].rank(pct=True)  # 低换手=高分位
```
帧**只按 `code` 索引**（无 `date` 维）→ 是**单日截面**，不存在可泄漏的时间维度。

### B2 pct40 出场分位 —— 只取末日截面
```python
di = S.ND - 1
sc = S.COMP_SUPER[di]
series = pd.Series(np.where(np.isfinite(sc) & S.ELIG_SUPER[di], sc, np.nan), index=...).dropna()
return (-series).rank(pct=True).to_dict()
```
`di = ND - 1` 为**最后一个可用日**，其后仅做截面 rank → 无未来行参与。

### C1 短线 KHunter —— 显式 `as_of` 截断
```python
if as_of is not None:
    _cut = pd.Timestamp(as_of)
    d = d[d['date'] <= _cut].reset_index(drop=True)
```
信号在**截断帧内**计算 → 即便内部含居中型窗口，帧本身已无未来数据。

## 三、静态红旗扫描（全生产脚本）

| 模式 | 命中 | 定性 |
|---|---|---|
| `cummax()` | 全项目 20+ 处 | **全部为回撤计算**（MDD 定义使然）→ 良性 |
| `shift(-N)` | `strategy_absorb_ev1/ev5/ev6/ev2` | **全部为前向标签**（`ret_fwd`/`ret5..20`），用于评估非决策 → 良性 |
| `bfill` / `backfill` | `data_index.py` / `update_daily.py` / `build_short_pool.py` / `fetch_close_westock.py` | **全部为注释/文档中的「数据回填」表述**，非 pandas `bfill` → 良性 |
| `center=True` / `fit_transform` / `StandardScaler` | 生产脚本 **0 命中** | — |
| `maximum/minimum_filter1d` 缺 `origin` | 生产脚本 **0 命中**（唯一命中在 R-tdx6 研究臂） | 已勘误 |

**未发现生产代码中的居中窗口、未来填充、全序列归一化。**

## 四、边界（必读）

- **审计通过 ≠ 无前视**：`audit` 为抽样（8 日）**只能证伪**；B/C 组仅有代码级结构论证，
  **未做实机截断审计**——其因子依赖 panel 之外的输入（`turn20` 需股本、`COMP_SUPER` 需 621MB factorlab 面板）
- A1 采用**列向抽样**（sub=6，≈681 只），理由=该性质逐股票独立；A2–A4 全列审计
- A3/A4 为**生产公式的忠实复刻**（panel 网格口径：panel 缺行为 NaN，生产为逐股票压缩序列，
  窗口构成略有差异——**不影响所测的 PIT 属性**）；**A1 审计的是真实函数对象，非复刻**
- 未覆盖：三轨 `lnatr`/`SUPER`/低波等因子的**原始定义位于 `backtest/` 研究脚本**内，
  生产侧只读其结果。要 audit 需先把定义重构为纯函数——**本轮不做**（重构属改动，须另立申报）

## 五、处置

- **零生产变更**（无 FAIL）
- 建议：把 `audit` 纳入因子/信号立项固定三步（见 `报告-factor_gate因子入库闸-20260918.md` §四）

## 六、产物

- `_audit_prod.py`（审计驱动，A 组四个包装器）/ `_tmp_0918_audit_prod.json`（原始读数）
- 协议冻结：`PRE-REGISTRATION_20260918_audit_prod.md`
