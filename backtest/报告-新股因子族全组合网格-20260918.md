# 报告 · 新股因子族「全组合 × 权重向量」网格穷举（R-subnew-grid-0918）

> 执行：2026-09-18 20:40 · 预注册 `PRE-REGISTRATION_20260918_subnew_grid.md`（含 5 条跑前修正）
> 性质：新臂，**n_trials = 359**，**零生产变更**

## 一、一句话结论

**0 / 359 格过门。** 扩展标的池（主板+双创+北交所，新股样本 3.4×）之后，假设仍然不成立——
而且这次不是"样本不存在"，是**效应本身在可交易窗口内为负**。

## 二、这轮做了什么（相对上一轮的三个实质变化）

| 项 | 上轮 | 本轮 |
|---|---|---|
| 宇宙 | 主板 4,087 只 | **全市场 6,493 只**（+双创 2,068 +北交所 342） |
| 面板内上市新股 | 1,072 只 | **3,634 只（3.4×）** |
| 次新股票日（≤250 日） | 264,126 | **880,253（3.3×）** |
| FR 覆盖 | 6,239,068 格 | **9,481,216 格**（+52%） |
| 因子 | 1 个（流通比例） | **9 个 × 双向** |
| 组合形式 | 单因子阈值筛 | 单/双/三因子 AND + **权重向量单纯形** |

**数据补齐**：新建研究侧抓取器 `_sn_fetch_ext.py` → `val_em_ext.csv`（**3,253,958 行 / 2,366 票**）。
**生产管线零改动**——`fetch_val_em_daily.py` 含显式「宇宙守护」（只延伸 val_em 已有票，注释明写
"追加会污染面板 NC 与生产轨B"），故新建独立通道而非改它。

## 三、核心结果

### 3.1 门槛：0 / 359

| 项 | 值 |
|---|---|
| 有效格 | 359 / 359 |
| **过门格** | **0** |
| 正超额格 | **4 / 359（1.1%）** |
| max t | **4.81**（Bonferroni 门 3.634） |

### 3.2 唯一越过 Bonferroni 的格是**孤立峰**

`fr+turn+amp`：exc_mean **+0.72%** / **exc_med −1.09%（负）** / t **+4.81**

| 子集 | exc_mean | t |
|---|---|---|
| 单 fr | −0.46% | −24.7 |
| 单 turn | −0.48% | −32.4 |
| 单 amp | −0.57% | −28.6 |
| 双 fr+turn | −0.28% | −11.0 |
| 双 fr+amp | −0.34% | −6.65 |
| 双 turn+amp | −0.00% | −0.05 |
| **三 fr+turn+amp** | **+0.72%** | **+4.81** |

成员与两两子集**全负**、仅三因子交集强正，且中位为负 ⇒ **右尾驱动的选择伪影**，
按预注册「孤立峰判噪声」**判废**。

### 3.3 因子画像：13 个「因子×方向」**全部为负**（θ=10%, H=5）

最不差 → 最差：`fr_low −0.44%` → `ret20_high −0.31%` → … → **`dp_low −1.01%`** → **`ret20_low −1.05%`**

三条读法：
- **用户方向（低流通比例）是 13 条里最不差的一条**（−0.44%）——**方向对，但幅度（44bp）不足以覆盖 20bp 往返（40bp）后的成本**，更别说 50bp
- **`dp_low`（上市最新）接近最差**（−1.01%）⇒ **"新股涨得猛"在可交易窗口内不成立**
- `ret20_low` 最差 ⇒ 次新域内**没有反转效应**

## 四、工程缺陷（本轮实测，4 项，全部已修或已申报）

1. **零价行 → `inf` 污染基准**：`ret1=C/C_prev-1` 遇 ext 面板零价行产生 `inf`，
   首跑 **359 格 exc_mean 全为 −inf、t 全 nan**（静默失败）。已加清洗重跑
2. **上市前占位行**：`bj920176` 首行 2026-07-13 全价 22.16 / volume=0，裸掩码只查 `C>0` 会误纳 → 加 `A>0`
3. 抓取 2,406 票中 **40 票无返回**（1.7%）→ 申报
4. 面板构建 **1 只 CSV 不可读** → 申报

## 五、边界（申报）

- **零候选 ⇒ 安慰剂与组合级未跑**（无适用对象）
- 分域（牛/熊）本轮**只作报告维度未展开**：因子画像已全线为负，分域不改变主结论
- 成本三档中 20bp 为主门；本报告数值均为 20bp
- FR 覆盖 2018-01 起（val_em 限制）；北交所 40 票缺
- 未做：H 敏感性（只对胜出格）——**因零胜出格，该步自然免除**

## 六、产物

`_sn_fetch_ext.py` / `val_em_ext.csv` / `_sn_panel_ext.py` / `_tmp_0918_sn_panel_ext.npz` /
`_sn_prep2.py` / `_tmp_0918_sn_mv.npz` / `_sn_grid2.py` / `_tmp_0918_sn_grid2.json` / 预注册（结果已回填）
