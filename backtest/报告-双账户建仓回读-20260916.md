# 报告 · 双账户建仓回读（R-single-track-0915 待验证项 ②）

**回读时间**：2026-09-16 16:16~16:20（定时任务 `automation-a5f093fe` 触发）
**回读对象**：`backtest/satellite_paper_a.json`（轨A 对照轨）/ `satellite_paper_b.json`（轨B 主轨）
**结论一句话**：**两账户均未建仓 —— 不是链失败，而是今日数据源全线降级导致补数范围不含三轨卫星池标的，建仓脚本判定"数据未到"而跳过；根因已定位（含证据），未做任何数据/参数改动。**

---

## 一、账户状态（回读实况）

| 项 | 轨B（主轨） | 轨A（对照轨） |
|---|---|---|
| role | 主轨 | 对照轨·零实盘资金 |
| basis | 68,000 | 17,000 |
| cash | 68,000.0 | 17,000.0 |
| 持仓 | **0 只** | **0 只** |
| nav_history | 0 行 | 0 行 |
| fills | 0 笔 | 0 笔 |
| 文件 mtime | 2026-09-15 20:56 | 2026-09-15 20:56 |

**与 basis 对比**：净值 = basis，收益率 +0.00%（因两账户尚未建仓，属"待建仓"状态，非异常）。

**建仓快照**：`satellite_paper_init.json` asof=2026-09-15（frozen_at 09-15），目标 = 轨A 10 只 / 轨B 20 只。

---

## 二、链状态：**正在运行，未完成**

```
pid=27604/38976  daily_refresh.py --force     起 15:54:50（由另一会话启动）
pid=6004/8036     └─ update_daily.py          起 15:54:52  ← 当前卡在这一步（已 25 分钟）
```

- **活跃度实测**：`update_daily` 子进程 CPU 1.6%、**读 IO 469KB/4s** → **在活跃工作，非卡死**
- **卡点**：`update_daily` 的「复权基准检测」（候选 400 只 → 逐只重拉验证）
- **日志**：`/tmp/dr_0916.log`（另一会话写入），最后写于 16:02，已修复 22 只北交所基准漂移（bj920021~bj920978）
- **链产物**：`satellite_pool.json` / `short_pool.json` mtime = 09-16 10:36（**今晨我手动跑的**，非本链产物）；`dual_system.html` = 09-15 21:51 → **链尚未走到看板重建与部署**

---

## 三、★ 根因：数据源降级 → 补数范围不含三轨卫星池 → 建仓受阻

### 3.1 证据链（逐条可复现）

**① 数据源全线滞后，自动降级**（`/tmp/dr_0916.log` 原文）
```
⚠️ TickFlow 源探样滞后（返回 2026-09-14，预期 ≥2026-09-16） → 回退新浪/腾讯
⚠️ 新浪源探样滞后（返回 2026-09-15） → 自动切换腾讯 qfq 降级源
数据源: tx | 最新交易日: 2026-09-16
降级源模式：仅补池内+跟踪池 408/7539 只（--all 可全量，约 1-2 小时）
```

**② 补数范围实测**：今日被写过的 `data_full/*.csv` 共 **431 个**（408 滞后清单 + 22 只 bj 修复 + 1 index），前缀分布 `sz30:133 / sz00:96 / sh60:91 / sh68:87 / bj:22`；`data_lag_list.csv` 共 **408 行**。

**③ 目标标的数据缺口**：建仓快照的 30 只标的中，**仅 5 只有 09-16 数据，25 只停留在 09-15**：
```
track_a（10 只）：仅 0~1 只有 09-16
  001213 中铁特货 = 2026-09-15   603053 成都燃气 = 2026-09-15
  601158 重庆水务 = 2026-09-15   601686 友发集团 = 2026-09-15
  002959 小熊电器 = 2026-09-15   600648 外高桥   = 2026-09-15
  000544 中原环保 = 2026-09-15   002932 *ST明德 = 2026-09-15
  603235 天新药业 = 2026-09-15   603869 ST智知  = 2026-09-15
track_b（20 只）：多数停留在 09-15（600668 尖峰集团、600363 联创光电 等）
```

**④ 建仓脚本因此判定"数据未到"**（只读 `--dry` 实测）
```
建仓快照: asof=2026-09-15（frozen）
数据截至 2026-09-15 | 池 asof 2026-09-15
!! 待建仓但数据未到（信号日 2026-09-15，最新数据 2026-09-15）——等收盘数据后自动执行
  track_a: 未建仓（待信号后首个交易日开盘执行），跳过净值记录
  track_b: 未建仓（待信号后首个交易日开盘执行），跳过净值记录
[dry] 未写盘
```

**⑤ 机制**（`satellite_paper_0914.py` 源码）
```python
sample = next(r["code"] for r in (pool["track_a"]["rows"] or [{"code":"sh600519"}]))   # = 001213 中铁特货
cal    = sorted(load_px(sample).keys())          # → 最新日 = 2026-09-15（该票无 09-16 数据）
auto_dates = [d for d in cal if asof and d > asof]   # asof=09-15 → auto_dates = 空
fill_date  = target_date or (auto_dates[0] if auto_dates else None)   # → None → 不建仓
```
**⇒ 交易日历取自"池内第一只票"，该票数据滞后即导致整体判定"数据未到"。**

### 3.2 缺陷定性

| 维度 | 判定 |
|---|---|
| 是否链故障 | **否** —— 链在正常运行，仅未跑完 |
| 是否数据事故 | **否** —— 已更新的数据本身正确（408 只成功 0 失败） |
| 是否设计缺陷 | **是（两处）** —— ①降级补数范围不含"三轨卫星池标的"；②建仓交易日历依赖单只样本票 |
| 影响面 | **所有数据源降级日**，卫星盘将持续无法建仓/调仓；持仓票缺当日数据时 mark 净值退化为旧价兜底 |
| 今日是否可挽回 | 链跑完后数据仍缺（update_daily 之后无补数步骤）→ **今日大概率不建仓**；除非手动补齐 30 只标的（**本次未做，遵纪律不擅改**） |

---

## 四、整手核对与跳过清单

**不适用** —— 两账户 0 持仓，尚未进入建仓环节，故无整手/跳过记录。（待建仓完成后补做。）

**参考**：按 `satellite_cfg.py`（CAP_B=68000 / 20 只 = 3,400 元/只）与今日已更新票价推算，轨B 建仓不会出现"一手买不起"（3,400 元可买 ≤34 元股票）；轨A 为名义账户（CAP_A=0），不产生真实买入。

---

## 五、看板回读（线上实证）

```
第1次: 1,002,729 字节  md5 096db799f1e3
第2次: 1,002,729 字节  md5 096db799f1e3   ← 两次一致，无 CDN 延迟
```
| 检查项 | 结果 |
|---|---|
| `sat-paper-b-card` | **2 次**（卡片 + 导航）✅ |
| `sat-paper-a-card` | **2 次** ✅ |
| `sat-paper-card`（旧合并卡） | **0 次** ✅ 已彻底移除 |
| 轨B 卡内容 | 基数 **68,000** · 主轨 · 净值 68,000 · +0.00% ✅ |
| 轨A 卡内容 | 基数 **17,000** · 对照轨·零实盘资金 · 净值 17,000 ✅ |

**注**：线上页面构建于 09-15 21:51（链未跑到部署步骤），内容与两个 json 一致，**无数据矛盾**。

---

## 六、异常与根因（汇总）

| # | 现象 | 根因 | 处置 |
|---|---|---|---|
| 1 | 两账户未建仓 | 数据源降级 → 补数仅覆盖 408 只池内+跟踪池 → 30 只目标标的 25 只无 09-16 数据 → 建仓脚本判定"数据未到" | **未改数据**；记录根因，待链完成/数据恢复 |
| 2 | 链卡在 update_daily 25 分钟 | 复权基准检测遍历 400 只候选（逐个重拉验证），IO 活跃非卡死 | 观察，不干预 |
| 3 | 数据源全线滞后 | TickFlow 返回 09-14、新浪返回 09-15 → 自动降级腾讯 qfq | 如实记录；如需可另查源可用性（未做） |

---

## 七、待办（建议，待拍板）

| 优先级 | 事项 | 说明 |
|---|---|---|
| **P1** | **把三轨卫星池标的纳入降级补数范围** | 现补数清单=短线池+跟踪池 408 只；建议加入 `satellite_pool.json` 的目标标的（含快照冻结的 30 只）→ 降级日也能 T+1 建仓 |
| **P2** | **建仓交易日历不再依赖单只样本票** | 改用 `index_000300.csv`（全市场日历）或池内"所有票日期并集" → 避免单票滞后导致整体不建仓 |
| P3 | 链完成后复读两账户 | 若链今日跑完仍无 09-16 数据 → 顺延至 09-17；需确认快照 asof=09-15 的时效性（信号已过期 2 日） |
| P4 | 数据源健康检查 | TickFlow/新浪连续滞后，建议核实源侧状态（本次未做，避免扩大范围） |
| P5 | 更新自动化任务 `automation-a5f093fe` | 本次为首次触发；若需持续回读，可改为每日 16:15 重复触发 |

---

## 八、纪律声明

- **只读为主**：未改任何数据文件、未重跑 `--init-fill`、未改配额/参数/引擎代码。
- **唯一写入**：本报告 + 记忆追加段（+ 一个只读诊断脚本 `backtest/_diag_chain_0916.py`）。
- 所有结论均附命令与真实输出，无"应该没问题"式表述。


---

## 九、补充观察（16:21 追加）

**① 链运行时长偏长，与历史先例可比**
`update_daily` 自 15:54:52 启动至 16:21 已运行 **26 分钟**，卡在「复权基准检测」。
历史先例（`daily_refresh.py` 源码注释，2026-09-15 修复记录）：
> *"本机代理（127.0.0.1:随机端口，VPN/Clash 类）会抖动；akshare 内部 requests 不带 timeout，
> 代理一挂/半死 → 复权检测整段永久阻塞（**0914 卡 52min / 0915 卡 76min 即此**）"*

本次已带 `NO_PROXY_ENV`（数据步禁代理）修复，但**仍偏慢**（实测 IO 469KB/4s 属活跃但低吞吐）。
**判定：尚未达到历史故障时长（52/76 分钟），继续观察；若超过 60 分钟无进展，应按历史根因复查（源侧半死请求而非代理）。**

**② 微信推送桥状态**：`bridge_status` 返回 `has_context_token: true`，但实际推送返回
`token 已失效(errcode -14)`。按任务纪律**未调用 `acquire_token`**（避免阻塞等待手机确认）→ **本次推送跳过**
（如需恢复推送，需在手机给 bot 发一条消息重新绑定 context_token）。

**③ 本次回读的时效边界**：结论建立在"链未完成"这一时点上；若链随后跑完并发生建仓，
须以链产出为准重做 §一/§四（本报告 §三 的根因分析不受影响——数据缺口在链跑完后依然存在）。
