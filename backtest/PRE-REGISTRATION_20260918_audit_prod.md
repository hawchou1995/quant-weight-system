# 审计协议冻结 · 2026-09-18 · 在产因子回溯前视审计（R-audit-0918）

> 冻结时间：2026-09-18 18:10（先冻结后跑）
> 性质：**排查（audit），非研究臂**——不产生策略结论、**不改动生产代码**，**n_trials = 0**。
> 动机：R-lookahead-0918 证明「前视缺陷可静默通过全部统计门」；当日只查了 tdx6 一族，
> **其余在产/在册因子的前视状态此前无任何证据**。本审计补齐。

## 一、审计对象清单（先冻结，7 项）

| 组 | 因子 | 在产位置 | 实现来源 | 可审计性 |
|---|---|---|---|---|
| A1 | A5 池B 趋势 `ADX14` | 打板 v1.3 池B | `a5_prod/paper_daban_a5.py:_adx14` | 纯函数，**直接审计真实实现** |
| A2 | A5 池A 超跌 `ret20` | 打板 v1.3 池A | 同上（`close[i]/close[i-20]-1`） | 纯函数 |
| A3 | A5 预筛 `rel_pos60` | 打板预筛 | 同上（60 日区间位置） | 纯函数 |
| A4 | A5 空间 `dh`(dist_high) | 打板预筛 F3 | 同上 | 纯函数 |
| B1 | 卫星池三低分 `score` | 轨A/轨B 选股 | `build_satellite_pool.py`（`amt`/`atr`/`turn20` 当日截面 rank） | 截面 rank → 截断不变性可测 |
| B2 | `pct40` 出场分位 | 轨A/轨B 出场（在产） | `pct40_exit_apply.py`（score 当日截面分位） | 同上 |
| C1 | 短线 KHunter 信号 | 短线池（在产） | `build_short_pool.py:_khunter_sig`（含 `as_of` 显式截断） | 结构性安全，做一次确认 |

## 二、方法（冻结）

1. **静态红旗扫描**（侦察已完成，结论见 §四）
2. **截断重算审计**（`factor_gate.py audit`）：对抽样日 i，`func(panel[:i+1])[i]` 必须等于 `func(panel)[i]`
3. **抽样口径申报**：per-stock Python 循环类因子（ADX 等）在**列向抽样**（`--sub`）下审计，
   步长 20（≈204 只），理由=**该性质逐股票独立**；其余因子全列审计

## 三、判据与处置（冻结）

- `mismatch_rate == 0` 且 `max_abs_diff <= 1e-6` → **PASS（本轮无前视证据）**
- 任一 FAIL → **报告 + 建议生产侧停用该因子**；**不擅自改动生产代码**（改动须另立变更申报）
- **审计通过 ≠ 无前视**（抽样只能证伪）；报告须显式声明该边界

## 四、静态扫描结论（侦察阶段已完成）

- `cummax()` 命中 **全部为回撤计算**（良性）
- `shift(-N)` 命中 **全部为前向标签**（`ret_fwd`/`ret5..20`，用于评估而非决策，良性）
- `backfill` 命中 **全部为注释中的"数据回填"表述**（非 pandas `bfill`）
- **未发现生产代码中的居中窗口 / 未来填充 / 全序列归一化**
- 生产代码两条设计模式天然 PIT 安全：① 短线用 `as_of` 显式截断帧 ② 卫星/pct40 用**当日截面 rank**
- 唯一真实命中位于 **R-tdx6 研究臂**（已勘误、已修、已隔离 `_buggy_lookahead_0918/`）

## 五、执行结果（跑后回填）

**执行**：2026-09-18 18:30。**总结论：在产因子未发现前视证据；零生产变更。**

| 组 | 因子 | 方法 | 结果 |
|---|---|---|---|
| A1 | A5 池B `ADX14` | audit，**直接调用生产函数对象** `paper_daban_a5._adx14`（sub=6，≈681 只） | **PASS** 0/8，max_diff 0.000e+00 |
| A2 | A5 池A `ret20` | audit 全列 | **PASS** 0/8，0.000e+00 |
| A3 | A5 `rel_pos60` | audit 全列 | **PASS** 0/8，0.000e+00 |
| A4 | A5 `dist_high` | audit 全列 | **PASS** 0/8，0.000e+00 |
| B1 | 卫星池三低分 `score` | 代码级证据：帧 `set_index("code")` 无时间维 → 单日截面 | **结构性安全** |
| B2 | `pct40` 出场分位 | 代码级证据：`di = ND - 1` 只取末日截面 | **结构性安全** |
| C1 | 短线 KHunter | 代码级证据：`d = d[d['date'] <= _cut]` 显式截断帧 | **结构性安全** |

**口径申报（与冻结的差异）**：
- A1 使用 `--sub 6` 列向抽样（冻结口径 §二.3 已预先申报），A2–A4 全列
- A3/A4 实现为**生产公式的忠实复刻**（panel 网格；生产为逐股票压缩序列，窗口构成略有差异，不影响 PIT 属性）；
  **A1 调用的是真实生产函数对象**，非复刻
- **B/C 组未做实机截断审计**（依赖 panel 之外输入：`turn20` 需股本、`COMP_SUPER` 需 621MB factorlab 面板），
  仅有代码级结构论证 → 已在报告 §四 显式声明为边界
- 未覆盖：三轨 `lnatr`/`SUPER`/低波因子的原始定义（在 `backtest/` 研究脚本内，生产侧只读结果）；
  重构为纯函数属改动，须另立申报 → **本轮不做**

**工具扩展申报**：为审计 per-stock Python 循环类因子，给 `factor_gate.py` 增加 `--sub`（列向抽样步长，默认 1）
与 `--sample`（抽样日数）CLI 参数。默认行为不变，不影响既有验收（selftest 未受影响）。

**n_trials 实际**：0（排查，非研究臂）。

**处置**：无 FAIL → **零生产变更**；未出具任何停用建议。

产物：`_audit_prod.py` / `_tmp_0918_audit_prod.json` / `报告-在产因子前视审计-20260918.md`

---

## 七、残留三项闭口（2026-09-18 21:40，本轮补齐）

首轮审计列出的三处空白**全部定位并审计完毕，且全部干净**：

| # | 残留项 | 定位结果 | 审计方法与结论 |
|---|---|---|---|
| 1 | `E = P["ext"]` 的面板构建者 | **`backtest/oss_0913/build_oss_panel_0913.py`**（122 行）——首轮在错误路径 `backtest/build_oss_panel_0913.py` 下找过，故未命中 | ① 红旗模式扫描 **0 命中**；② 逐行确认**全部**用 `pd.DataFrame(...).rolling(n, min_periods=n)`（pandas 默认 `center=False` = **尾随**）；③ **脚本自带对拍断言**（行 113-114）：手工按 `[i_t-n+1, i_t]` 尾随窗口算 BBI 与面板值 `assert abs(...) < 1e-6` ⇒ **PIT 安全且自证** |
| 2 | `lnatr` 因子定义 | **`backtest/signal_satellite_0913.py:53 signal_ln_atr()`**（轨A 主因子；证据链：`exit_shadow_0915.py:4,29` 与 `build_satellite_pool.py:41` 的 `ln_atr` 键） | 结构性 PIT 安全：`d.index[-1] != LAST_DAY` 强制**当日有行情**；`close.shift(1)` 取前日；`tail(70)/tail(20)` **尾随窗口**；`np.log(amt20).rank() + atr20.rank()` 是**当日单截面 rank**（df 为每票一行）——与卫星三低分/pct40 同一安全模式 |
| 3 | factorlab builder（SUPER 的 `FD` 基本面来源） | `backtest/factorlab_0913/build_factor_panel_0913.py`（217 行） | 红旗模式扫描 **0 命中**（首轮已做，此处归档） |

**结论：R-audit-0918 在产因子回溯前视审计全项完成，零 FAIL，零生产变更。**
累计覆盖：A 组（A5 打板族 4 项实机 audit）+ B/C 组（卫星三低分 / pct40 / 短线 KHunter 代码级结构证据）
+ 本轮三项残留。

**边界重申**：除 A 组四项为**实机截断审计**外，其余均为**代码级结构证据**——
**结构证据能证明「没有该模式的缺陷」，不能证明「绝无前视」**。该边界不因本轮补齐而消除。

**旁证（非本审计职责，但值得记录）**：`build_oss_panel_0913.py` 自带尾随对拍断言，
说明项目在**关键面板构建器**上已有自证习惯；建议把这一习惯推广到**所有**因子脚本
（对照 `factor_gate.py audit` 的截断重算，两者互补：前者验窗口方向，后者验全局因果）。
