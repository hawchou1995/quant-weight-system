# 预注册 · 2026-09-19 · 反向筛接入 A5 打板生产（R-daban-negscreen-0919）

> 冻结时间：2026-09-19（先冻结后动生产；用户「批准」= 修掉我倒打的双标）
> 由来：R-g52negscreen-0919 影子对拍显示，把 294/496 笔 v1.3 入场剔除后：
> **每笔 +0.768%→+1.774%、满仓年化 +4.12%→+6.94%、夏普 0.292→0.435、回撤 −48.63%→−36.81%、交易 393→163**。
> 我此前用「门① t=−1.07 未达 −2.0」把它归档——**而 A5 生产系统自身显著度只有 +0.17 就在实盘跑**。
> **双标站不住。本轮修正：把反向筛接进生产扫描器，拦新开仓。**

## 一、资产与口径（冻结）

- **回避掩码**：`_tmp_0919_g52_block9.npz`（9 强臂回避集并集，已建；kv 面板 2599×4087，2016-2026）
- **9 强臂**（g157a 已剔除，最弱）：g300 / g177a / g093d / g121a / g83a / g205g / g93a / g200b / g134a
- **回避窗**：臂信号日 s 起，该股在 **[s, s+K+1]** 回避（K=各臂出场上限 1~10 日）；
  入场日 D 命中 ⇔ ∃臂信号 s≤D−1 且 D−s≤K+1（只用已收盘信号，**无前视**）
- **作用面**：只拦**新开仓**；在仓票按原规则出场（Q1 拍板）
- **不覆盖的情形**：候选码不在 kv 面板内（北交所/已退市/缺失）→ **不拦**（保守，宁漏不错）

## 二、接入方式（最小侵入）

- 挂点：`a5_prod/paper_daban_a5.py` **阶段2 阶段2 的 gap 判定之后、建 pos 之前**（约 L458）
- 命中 → **不入场** + 打日志 `[反向筛剔除] <code> <name> D <date> 命中臂=...`
- **不改**任何既有常量、不改 A2_tp3 回避清单、不改出场逻辑
- 加总开关 `REV_SCREEN = True`（本轮拍板开启）；关掉即恢复原行为

## 三、验收判据（冻结，TV-F 跟到底）

**工程验收**：
1. 改动前后**未命中反向筛的交易日与入场必须完全一致**（用影子回放对拍：对同一批历史日期，
   关开关 vs 开开关，未命中票的进出场在逐笔层面一致）
2. 命中必须**只发生在已收盘信号之后**（无前视）——用 L458 前 D 日收盘数据做一次时序对拍
3. 面板未覆盖码不会被误拦（构造一个北交所码用例）

**业务验收**（接生产后按月累计，**不单独设统计门槛**——盖因生产系统自身也不显著）：
4. 按月记录：命中笔数、被剔票的 T+2 收益（应为负方向，与影子对拍同向）
5. 季度复判：若被剔票的实际 T+2 收益转正，则关停并回归

## 四、风险与回退

- **回退**：`REV_SCREEN = False`，一行常量；保留日志，不影响历史状态文件
- **备份**：`a5_prod/paper_daban_a5.py` 已有 `D:/Tools/cache/mem_bak_0918/` 备份位，改动前再存一份
- **警示**：本改动**不声称提升显著度**，理由是「低换手 + 去弱标」的**成本与回撤改善**（满仓回撤 −48.6%→−36.8%、
  交易降 58%），不是"提高了预测准确率"。

## 五、n_trials 申报

本轮无新参数搜索：9 臂、各臂 K、envelopes 全来自 R-g52* 已冻结定义，0 增量。

---

## 六、执行结果回填（2026-09-19）

### 已完成的接线

| 项 | 状态 |
|---|---|
| 生产扫描器挂点（3 处） | ✅ `打板系统A5实验_20260827/paper_daban_a5.py`（**不是 `a5_prod/` 镜像副本**——`daily_refresh.py` 实际跑的是前者）
| 闸门模块 `rev_screen.py` | ✅ 已放入生产目录；资产路径改绝对路径+4 候选，放任何位置都能定位
| 拦单位置 | ✅ 一字板判断之后、建 pos 之前（L463）
| 日志 | ✅ 当日回避规模 + A2_tp3 清单并列打印
| 环境变量开关 | ✅ `REV_SCREEN=0/1`（留 1；验收脚本化对比用）

### 验收（预注册 §三）

| 项 | 结果 |
|---|---|
| 模块在生产目录可导入、资产可定位 | ✅ |
| 面板未覆盖码（北交所）不误拦 | ✅ `hit(bj920176)=False` |
| 覆盖缺口**显式告警**（不再静默失效） | ✅ 见下方实测输出 |
| 关/开闸进场严格包含 | ⚠️ **未测成**——因资产未覆盖最新日，两边都是 0 进场，对拍为空（见下） |

**实测输出（生产扫描器干跑）**：
```
  !! 反向筛闸门告警: 2026-09-18 不在资产覆盖内（资产至 2026-09-11，缺口 7 天）→ 本轮闸门失效，需重生成资产
```

### ⚠️ 未完成项（闸门当前**实际失效**）

回避资产 `_tmp_0919_g52_block9.npz` 只覆盖到 **2026-09-11**，
而 `data_full` 已到 **2026-09-18**（差 7 个交易日），扫描器处理的正是最新日 →
**当前一单都不会拦。**

我已把"静默不拦"改成"显式告警"，但**要真正生效，必须让资产每日再生成为日链的一步**

### 交割过程中修掉的我方缺陷（4 个，全部会致命）

1. **`import rev_screen` 漏加**——首版补丁的 fallback 替换没生效，`rev_screen.hit()` 会 NameError
2. **改错了副本**——先改的是 `quant-weight-system/a5_prod/paper_daban_a5.py`（镜像），
   而生产跑的是 `quant-weight-system/../打板系统A5实验_20260827/paper_daban_a5.py`。已还原镜像副本
3. **`def stats(date)` 遮蔽 `datetime.date`**——构造日差失败被 `except` 静默吞成 −1，导致"过期"永远判不出。已改用 `_date` 别名并去掉兜底吞异常
4. **`--date` 文档说支持但代码没解析**——导致验收无法指定历史日（对拍为空）

### 判定

**接线完成，但闸门当前不生效。** 交付为：
- 代码与机制已就位、可逆（`REV_SCREEN=0` 一行回退）、**备份 `D:/Tools/cache/mem_bak_0918/paper_daban_a5_prod_pre_revscreen.py`**
- **但需新增"每日重生成回避资产"这一步才能生效**——属新的生产改动，**需另行拍板**
